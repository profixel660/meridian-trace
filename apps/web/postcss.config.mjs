// Tailwind v4 uses the dedicated PostCSS plugin package.
// See: https://tailwindcss.com/docs/installation/using-postcss
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
